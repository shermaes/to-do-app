function Reducer(state, action) {
    switch (action.type) {
        case 'add-note':
            return state;
    
        case 'remove-note':
            return state;

        case 'update-note':
            return state    
    }
}

export default Reducer